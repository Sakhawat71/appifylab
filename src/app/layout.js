
import "./globals.css";


export const metadata = {
  title: "CodeLearn",
  description: "CodeLearn for applify",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        
      >
        {children}
      </body>
    </html>
  );
}
